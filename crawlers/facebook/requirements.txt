# Crawler runtime
playwright>=1.44,<2.0
PyYAML>=6.0,<7.0

# DeepSeek rental-extraction pipeline (src.rental_pipeline)
openai>=1.0,<3.0
python-dotenv>=1.0,<2.0

# Dev / testing (optional)
pytest>=8.0,<9.0
