# Alonhadat Crawling Methodology

**Superseded.** This documented an earlier implementation (a `BrowserFetcher`/
Playwright-driven scraper against alonhadat's pre-redesign `.html` URL scheme)
that no longer matches either the site's current markup or the code in
`src/crawl/run_alonhadat.py`. See **`docs/alonhadat_scraping.md`** for the
current implementation, including what changed and why (§3, "What changed
from the previous implementation").
